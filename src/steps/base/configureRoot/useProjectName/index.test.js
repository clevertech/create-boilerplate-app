describe('Configuration of project name', () => {
  it('makes sure all boilerplate name references in makefiles are overwritten with answer value', () => {
    expect(false).toBe('Some method for checking this')
  })
})
