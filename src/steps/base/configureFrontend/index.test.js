// Configure Frontend
describe('Configuration of Frontend', () => {
  it('sets project name', () => {
    // setup project name (Dockerfile, Makefile, ...)
    expect(false).toBe(true)
  })

  it('also configures env file (database connection, etc.)', () => {
    // NOTE: chokidir_usepolling if linux?  Maybe this isn't an issue ... TBD
    expect(false).toBe(true)
  })

  it('also configures package.json file', () => {
    // setup package.json file
    expect(false).toBe(true)
  })

  it('cleans up after itself', () => {
    // delete git dir
    expect(false).toBe(true)
  })
})
