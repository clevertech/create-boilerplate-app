export const run = async function(answers) {
  //   // copy to .env
  //   await fs.copy(
  //     path.join(basedir, 'api/.env.example'),
  //     path.join(basedir, 'api/.env')
  //   )
  // }
  return answers
}
